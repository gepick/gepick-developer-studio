const routes = {
  statistics: '/statistics',
  betSimulation: '/bet-simulation',
  picks: '/picks',
  matches: '/matches/:leagueId?',
  match: '/matches/match/:matchId',
  other: '/*',
}

export default routes
